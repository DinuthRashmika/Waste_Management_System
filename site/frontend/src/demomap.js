import React from 'react';

const DemoMap = () => {
    return (
        <div style={{ width: '100%', height: '300px', backgroundColor: '#e0e0e0', borderRadius: '8px', display: 'flex', alignItems: 'center', justifyContent: 'center', marginBottom: '20px' }}>
            <p style={{ color: '#555', fontSize: '18px', textAlign: 'center' }}>
                Demo Map Placeholder
                <br />
                (Replace with actual map later)
            </p>
        </div>
    );
};

export default DemoMap;